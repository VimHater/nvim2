# 💤 LazyVim

# Windows
```
rm -Force ~\AppData\Local\nvim
rm -Force ~\AppData\Local\nvim-data
git clone https://github.com/VimHater/nvim2.git "$env:USERPROFILE\AppData\Local\nvim"

```

# Linux
```
rm -rf ~/.config/nvim
rm -rf ~/.local/state/nvim
rm -rf ~/.local/share/nvim
git clone https://github.com/VimHater/nvim2.git ~/.config/nvim

```
